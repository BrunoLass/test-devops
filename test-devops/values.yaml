replicaCount: 1

service:
  type: ClusterIP
  port: 8080

image:
  repository: nginx
  tag: "1.16.0"

resources:
  limits:
    cpu: "500m"
    memory: "256Mi"
  requests:
    cpu: "250m"
    memory: "128Mi"
